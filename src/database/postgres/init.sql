CREATE TABLE IF NOT EXISTS users(id bigserial primary key, email varchar(255), lastName varchar(255), firstName varchar(255), password varchar(255), IsDeleted boolean DEFAULT false,
CONSTRAINT uc_email UNIQUE (email));